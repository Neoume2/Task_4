# Example environment variables for the server
# Copy to `.env` and fill with real values before running locally.

# MongoDB connection string (do NOT commit real credentials)
MONGO_URI=mongodb://<username>:<password>@cluster0-shard-00-00.example.net:27017,cluster0-shard-00-01.example.net:27017,cluster0-shard-00-02.example.net:27017/?ssl=true&replicaSet=atlas-xxxxx-shard-0&authSource=admin&retryWrites=true&w=majority&appName=Cluster0

# JWT secret used to sign tokens
JWT_SECRET=replace-this-with-a-strong-secret
